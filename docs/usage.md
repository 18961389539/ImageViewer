# 使用说明

## 1. 打开图像

1. 在 demo 程序中点击“打开图像”、按 `Ctrl+O`，或在宿主程序中调用控件的打开图像工作流。
2. 选择 `png`、`jpg`、`jpeg`、`bmp`、`tif` 或 `tiff` 文件。
3. 图像载入后，可使用适配视图、实际尺寸、缩放到选中 ROI、重置视图等功能进行浏览。

在 demo 中点击“打开体数据”可以一次选择多个图像切片。程序会按文件名排序并检查切片尺寸；加载后可在三维视图、轴位、冠状和矢状视图之间切换。冠状和矢状视图支持底部切片滑块、上一层/下一层按钮、鼠标滚轮以及方向键切层。

## 2. 绘制与管理 ROI

1. 使用“绘制”菜单添加区域类 ROI，例如矩形、圆、椭圆、多边形或折线。
2. 使用“测量”菜单添加长度、折线、面积、角度、卡尺、点坐标、自动边缘吸附点、自动圆测量、圆心距、三点圆、拟合椭圆和梯度检测对齐等测量或分析辅助工具。折线测量会显示总长度、线段数以及最短/最长线段；面积测量要求闭合多边形，并显示面积、周长、顶点数和质心。选择“点坐标测量”后单击即可记录坐标；选择“自动边缘吸附点”后在目标边缘附近移动鼠标，系统会实时预览候选边缘点、置信度和梯度分数，点击后提交当前预览位置；系统沿多个方向搜索局部梯度峰并定位到亚像素边缘。选择“自动圆测量”后在目标圆附近单击，系统会自动搜索边缘并生成可继续编辑的圆测量 ROI；“圆心距”依次选择两个圆；“三点圆”连续点击三个不共线点。
3. 通过 ROI 列表可查看当前对象、切换可见性、切换锁定状态；删除所选 ROI 或清空全部 ROI 后，可使用 `Ctrl+Z` 恢复。
4. 通过右键菜单可删除选中 ROI、清空全部 ROI、编辑属性、设置标签、设置颜色。

## 3. 测量与分析

1. 当图像和 ROI 都已准备好后，可在“分析与导出”菜单中查看分析摘要或导出 CSV。
2. 实时分析相关开关被收拢在“分析运行”子菜单中，避免常用操作被大量高级选项淹没。
3. 大图与缓存相关选项位于“大图与性能”菜单；更少用的缓存与 GPU 选项位于“高级性能”子菜单。
4. 大于约 400 万像素的图像会自动启用适合尺寸的多分辨率金字塔、分块缓存和预取策略，加载后仍可在性能菜单中手动调整。
5. 伪彩相关行为分为两层：常用调色板切换，以及“伪彩策略”中的实现策略开关。
6. 右键菜单打开后可直接输入搜索命令；按向下键进入第一个匹配项，按 `Esc` 先清空搜索，再按一次关闭菜单。图像浏览时可按 `Ctrl+F` 适应窗口。
7. 图像加载和操作错误会以可访问的状态提示通知；错误提示中的关闭按钮具有明确名称，屏幕阅读器可读出错误内容和加载进度。
8. 体数据分割完成后，点击“接受”会把候选结果转换为 Blob ROI，切换到当前切片的二维视图并纳入撤销栈；点击“拒绝”不会修改 ROI。

## 4. 标定与物理单位

1. 选择合适的线类 ROI 后，可使用“像素标定”设置像素尺寸与物理单位。
2. 标定完成后，长度、面积和 CSV 指标会按物理单位输出。
3. 如果未设置物理单位，系统默认使用 `px`。

## 5. 保存、加载与项目交付

1. `保存 ROI`：仅保存 ROI 集合与标定信息，适合算法结果交换。
2. `保存会话`：保存图像路径、ROI、标定和当前视图状态到 `.ivsession`。
3. `打开项目或会话`：可打开 `.ivsession` 或 `.ivpkg`。
4. `导出项目包`：生成 `.ivpkg`，把会话和图像资产一起打包，适合移交他人或归档。
5. 会话发生图像、ROI、标定或视图变化后，底部状态栏显示“未保存”；可按 `Ctrl+S` 保存会话。如果上次运行留下自动保存内容，启动后会显示“恢复”入口；恢复后请另存为正式会话或项目包。
6. 导出分析 CSV 时会同时生成同名 `.metadata.json` 文件，包含源图像 SHA-256、尺寸、时间戳、版本、标定、渲染设置和完整 ROI 文档，可用于复核结果来源。

## 6. 常见建议

- 如果需要发给他人复现当前界面状态，优先使用 `.ivpkg`，因为它可以一起带上图像资产。
- 如果只需要交换 ROI 结果，优先使用 ROI JSON，而不是整个会话包。
- 若卡尺参数较多，优先查看主区域中的常用参数；“高级参数”折叠区只在需要微调时展开。

## 7. 宿主程序集成

1. 如果你的 WPF 宿主已经使用依赖注入，优先在应用启动时调用 `AddImageViewerHost()`，让 `ImageViewerHost` 和 `ImageViewer` 由容器创建。
2. 如需替换宿主调度策略，可在注册 `AddImageViewerHost()` 之前覆盖 `IImageViewerRefreshSchedulerFactory`、`IImageViewerLatestTaskSchedulerFactory`、`IImageViewerPeriodicTaskSchedulerFactory`。
3. 如需接管非关键错误日志，可覆盖 `IImageViewerAnalysisDiagnostics`，把分析异常统一接入现有日志或遥测系统。
4. 如果宿主需要自定义 ROI 注册表或额外组合逻辑，可通过 `AddImageViewerHost(builder => { ... })` 配置 `ImageViewerHostBuilder`。
